/**
 *
 */
export const ImageTemplate = (): string => {
  return `
    /**
     * A collection of images used by the project.
     */
    export default Object.freeze({
      // backArrow: require('./backArrow.png')
    });
  `;
};
